platform: nodejs
platformVersion: "20"
buildCommand: "export NODE_PATH=/usr/local/lib/node_modules:$NODE_PATH && echo $NODE_PATH && which npm && npm run build"
autoDeploy: true

static:
  directories:
    - build
  spaFallback: true
  headers:
    "*.js": "public, max-age=31536000, immutable"